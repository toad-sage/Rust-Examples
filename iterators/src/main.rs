use iterators::using_other_iterator_methods;

fn main() {
    // let v1 = vec![1, 4, 2];

    // let v2: Vec<_> = v1.iter().map(|x| x + 2).collect();
    // println!("{:?}", v2);

    using_other_iterator_methods();
}
